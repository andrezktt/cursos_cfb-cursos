"use strict" // MODO RESTRITO

let nome = `André` // a variavel deve ser identificada
console.log(`CFB Cursos`) // podemos escrever strings
console.log(nome) // podemos chamar variáveis
console.log(`Nome: ${nome}`) // e podemos concatenar as duas